# Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Secrets — never commit
terraform.tfvars

# Lambda build artifacts
lambda/auth.zip

# macOS
.DS_Store
